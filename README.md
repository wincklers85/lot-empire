# LOT EMPIRE v8 FIXED STARTUP

Versione corretta per Render e smartphone. CSS e JavaScript principali sono inclusi direttamente in index.html per evitare file vecchi in cache o script non caricati. La cartella assets non è stata spostata.

Render: Web Service, Build `npm install`, Start `npm start`. Dopo il caricamento usare Clear build cache & deploy.
